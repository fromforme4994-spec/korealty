# 작업 지침

코리얼티디엠씨㈜ 기업 사이트. 정적 HTML/CSS/JS, 빌드 과정 없음.
구조와 파일 목록은 README.md 에 있다. 여기에는 **모르면 깨뜨리는 것들**만 적는다.

## 배포

- `main` 에 push 하면 Vercel 이 배포한다. 별도 명령 없음.
- `vercel.json` 의 `cleanUrls: true` — `card.html` 을 올리면 주소는 `/card` 다.
- HTML 은 `max-age=0, must-revalidate` 라 새로고침하면 항상 최신이다.

### 캐시 (vercel.json)

| 대상 | 기간 | 이유 |
|---|---|---|
| `assets/fonts/` · `assets/vendor/` | 1년 immutable | 파일명 뒤 `?v=` 로 버전이 바뀐다 |
| `assets/img/` | 7일 + stale-while-revalidate | 사진을 같은 이름으로 갈아 끼워도 일주일이면 따라잡는다 |
| `assets/video/` · `assets/docs/` | 30일 | 거의 바뀌지 않고, 무겁다(영상 6.5MB · 지명원 12MB) |
| `css/` · `js/` | **캐시하지 않음** | 주소에 버전이 없다. 길게 캐시하면 사용자가 옛 코드에 갇힌다 |

**CSS·JS 를 길게 캐시하려면 먼저 주소에 `?v=` 를 붙이는 장치가 있어야 한다.**
지금은 없으므로 must-revalidate 로 둔다(304 만 오가므로 실제 전송은 없다).
예전에 `/assets/*` 전체를 1년 immutable 로 돌렸다가, 사진이 없던 시절의 404
응답까지 1년 박힌 사고가 있었다(js/data.js 주석 참고). 그래서 보수적으로 간다.
- 배포는 되돌리기 쉽지만 외부에 나가는 일이다. 지시가 없으면 push 전에 확인받는다.

## 폰트 — 가장 흔한 함정

화면에 새 글자를 쓰면 **반드시** 서브셋을 다시 만든다.

```
python3 tools/subset-fonts.py
```

Pretendard 를 사이트에 실제로 등장하는 글자만 남겨 서브셋해 쓴다.
안 돌리면 새 글자가 두부(□)로 뜬다. 스크립트가 루트의 모든 `*.html` 과
`js/`, `css/` 를 훑으므로, 루트에 HTML 을 새로 추가하면 자동으로 포함된다.

폰트 파일명은 그대로인데 CDN 이 1년 캐시하므로, 같은 스크립트가 HTML·CSS 의
`?v=` 를 내용 해시로 갱신한다. 이 값을 손으로 건드리지 않는다.

## 도구

| 스크립트 | 언제 |
|---|---|
| `tools/subset-fonts.py` | 문구를 바꾼 뒤 (필수) |
| `tools/build-sitemap.py` | 배포 전. git 에서 페이지별 수정일을 읽어 sitemap 갱신 |
| `tools/build-portfolio.py` | `js/data.js` 의 실적을 고친 뒤. 크롤러용 정적 카드를 HTML 에 씀 |
| `tools/build-og-card.py` | `/card` 의 카톡 미리보기 이미지. playwright + pillow 필요 |

## 색

`css/style.css` 의 CI 색을 기준으로 삼는다. 새로 만들지 않는다.

- `--navy #293E4B` · `--navy-deep #1B2B35` · `--red #E23D2C`
- **CI 레드는 흰 배경에서 대비가 4:1 언저리다.** 작은 글자에 그대로 쓰면
  기준(4.5:1)에 못 미친다. 글자용으로는 더 진한 빨강을 따로 쓴다.
  아이콘·구분선 같은 그래픽 요소에는 CI 레드를 그대로 쓴다.

## 확인 방식

이 저장소의 관행은 **헤드리스로 렌더해서 눈으로 확인**하는 것이다.
playwright 가 설치돼 있다. 색이나 대비를 바꿨으면 계산값이 아니라
**렌더된 픽셀에서 배경색을 읽어** 대비를 잰다. 반투명 패널이 겹치면
계산값과 실제가 다르다.

## 3D 지도 (js/hero-map.js, js/portfolio-map.js)

- MapLibre GL JS 4.7.1 을 `assets/vendor/` 에 직접 넣어 쓴다. 타일은 openfreemap.
- 데스크톱 + WebGL 일 때만 뜬다. 아니면 히어로는 영상, 실적은 기존 SVG 가 남는다.
- 실패에 대비해 되돌리는 경로(`revert()`)와 10초 안전장치가 있다. 지우지 않는다.
- **건물은 줌 13 부터만 타일에 있다.** 전체 조망에서는 건물이 없다.
- **`fill-extrusion` 에 외곽선 속성이 없다.** 규격 자체에 없어서 버전을 올려도
  안 된다. 건물 바닥 외곽선은 별도 `line` 레이어로 그린다. 입체의 세로·지붕
  모서리는 이 방식으로 나오지 않는다.
- 히어로 지도는 흰 글자가 얹히는 배경이라 어둡게 유지한다. 밝게 바꾸지 않는다.

## /card (대표 디지털 명함)

- 사이트 CSS 를 쓰지 않는 독립 페이지다. `card.html` 안에 스타일이 다 있다.
- 링크로만 전달한다. 메뉴·푸터 어디에도 걸지 않고 `noindex` 다. 사이트맵에도 없다.
- 인트로 배경과 카톡 미리보기 배경은 **같은 값이어야 한다.** 한쪽을 바꾸면
  `tools/build-og-card.py` 도 같이 바꾼다.
- 미리보기를 바꾸면 `card.html` 의 `og:image` 뒤 `?v=` 를 올린다.
  그래도 카카오가 자체 서버에 캐시하므로 사용자가 초기화 도구를 돌려야 한다.
  이건 코드로 못 푼다.

## 커밋

한국어로 쓴다. 무엇을 했는지가 아니라 **왜 그렇게 했는지**를 남긴다.
특히 다른 선택지를 버린 이유. 제목은 명령형 한 줄.
